import DeltaObject from './DeltaObject';

/**
 * @typedef {function} DeltaCreator
 * @param {...*} varargs - Variable arguments required to generate the delta.
 * @returns {DeltaObject} Delta object.
 * @see {@link DeltaObject}
 */
